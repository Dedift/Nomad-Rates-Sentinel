spring:
  config:
    activate:
      on-profile: reactive

  main:
    web-application-type: reactive

  r2dbc:
    url: r2dbc:postgresql://localhost:5432/nomad_rates
    username: postgres
    password: postgres
