server:
  port: ${PORT:8081}

spring:
  sql:
    init:
      mode: always

  application:
    name: Nomad Rates Sentinel

  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://dpg-d6tvpthaae7s73e6uu60-a:5432/nomad_rates?connectTimeout=3&socketTimeout=10&tcpKeepAlive=true}
    username: ${SPRING_DATASOURCE_USERNAME:nomad_rates_user}
    password: ${SPRING_DATASOURCE_PASSWORD:ycYCPEpHCFKaQd1GJ4hJXl0j7eOmcfOl}
    hikari:
      maximum-pool-size: ${SPRING_DATASOURCE_HIKARI_MAXIMUM_POOL_SIZE:20}
      minimum-idle: ${SPRING_DATASOURCE_HIKARI_MINIMUM_IDLE:4}
      connection-timeout: ${SPRING_DATASOURCE_HIKARI_CONNECTION_TIMEOUT:3000}
      validation-timeout: ${SPRING_DATASOURCE_HIKARI_VALIDATION_TIMEOUT:2000}
      initialization-fail-timeout: ${SPRING_DATASOURCE_HIKARI_INITIALIZATION_FAIL_TIMEOUT:5000}

  threads:
    virtual:
      enabled: true

rates:
  adapters:
    request-timeout-ms: 30000
    nbk:
      url: https://nationalbank.kz/en/exchangerates/ezhednevnye-oficialnye-rynochnye-kursy-valyut
    xe:
      url: https://www.xe.com/currencyconverter/convert/?Amount=1&From=USD&To=KZT
  cache:
    sync-ttl: ${RATES_CACHE_SYNC_TTL:0s}
